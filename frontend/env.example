# Frontend environment variables
# Create a copy of this file named '.env' in the same directory (frontend folder)
# Replace the values with your actual credentials

# Google OAuth Client ID (same as the one in backend)
VITE_GOOGLE_CLIENT_ID=your_google_client_id

# API URL (only needed if your backend is on a different domain)
VITE_API_URL=http://localhost:3000
